// Package main contains shared types for all memory pattern examples
package main

// Student represents a student record used across all examples
type Student struct {
	ID    int64
	Name  string
	Grade float64
	Age   int
}
